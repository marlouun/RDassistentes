# Teste de atendimentos abertos

Este branch altera temporariamente o filtro do RD Reports de `type=operators` para `type=customers` para validar qual agrupamento corresponde melhor à lista operacional "Meus atendimentos" da RD Conversas.

O endpoint continua sem persistir contatos no D1.
